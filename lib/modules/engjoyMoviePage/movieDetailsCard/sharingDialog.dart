import 'package:flutter/material.dart';

class SharingDialog extends StatelessWidget {
  const SharingDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
